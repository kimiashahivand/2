long long C4(int n, int k) {
    if (k > n) return 0;
    if (k > n - k) k = n - k; // C(n,k) = C(n,n-k)
    
    long long result = 1;
    for (int i = 1; i <= k; i++) {
        result = result * (n - k + i) / i;
    }
    return result;
}
